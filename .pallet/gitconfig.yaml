apiVersion: github.coop.no/v1alpha1
kind: RepositoryConfig
metadata:
  name: dot-github
spec:
  description: "Global GitHub repository"

  branches:
    default: main
    protection:
      - id: main
        pattern: main
        requireSignedCommits: false
        requiredStatusChecks:
          checks:
            - 'policy-bot: main'
        requiredPullRequestReviews:
          requireCodeOwnerReviews: false
  visibility: public
